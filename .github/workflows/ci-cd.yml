name: CI/CD for Docker

on:
  push:
    branches: [ "master" ]   # hoặc "master" nếu repo bạn dùng master

jobs:
  build:
    runs-on: self-hosted #ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Verify Docker login
        run: docker info

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/python_todolist:latest .

      - name: Push Docker image
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/python_todolist:latest
        
  #deploy:
  #deploy to render.com
  #  needs: build
  #  runs-on: ubuntu-latest
  #  steps:
  #    - name: Trigger Render Deploy Hook
  #      run: |
  #        curl -X POST "${{ vars.RENDER_DEPLOY_HOOK }}"
   # runs-on: self-hosted   # chạy trên runner local
   # needs: build
   # steps:
      #- name: Checkout repo
     #   uses: actions/checkout@v3
        
    #  - name: Configure kubectl
    #    run: echo "KUBECONFIG=$HOME/.kube/config" >> $GITHUB_ENV

     # - name: Deploy to Kubernetes
    #    run: |
      #    kubectl set image deployment/python_todolist python_todolist=anhnguyen1988/python_todolist:latest --record
        #  kubectl rollout status deployment/python_todolist#